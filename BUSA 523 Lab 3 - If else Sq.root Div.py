#!/usr/bin/env python
# coding: utf-8

# In[1]:


import math

# Ask user for Input
x = float(input("Enter the value of x: "))
y = float(input("Enter the value of y: "))

# Multiply x and y, and put the result in variable z
z = x * y

# Check if z is greater than 50
if z > 50:
    # Take the square-root of z and put it in a variable called Zsq
    Zsq = math.sqrt(z)
    
    # Check if Zsq is an even number
    if Zsq % 2 == 0:
        print("Even Square Root")
    else:
        print("Odd Square Root")
# Check if z is 
else:
    # Divide z by 2 and put the result in variable Zdiv
    Zdiv = z / 2
    
    # Check if Zdiv is an even number
    if Zdiv % 2 == 0:
        print("Even Division")
    else:
        print("Odd Division")


# In[2]:


import math

# Input two numbers (x and y) from the user
x = float(input("Enter the value of x: "))
y = float(input("Enter the value of y: "))

# Multiply x and y, and put the result in variable z
z = x * y

# Check if z is greater than 50
if z > 50:
    # Take the square-root of z and put it in a variable called Zsq
    Zsq = math.sqrt(z)
    
    # Check if Zsq is an even number
    if Zsq % 2 == 0:
        print("Even Square Root")
    else:
        print("Odd Square Root")

else:
    # Divide z by 2 and put the result in variable Zdiv
    Zdiv = z / 2
    
    # Check if Zdiv is an even number
    if Zdiv % 2 == 0:
        print("Even Division")
    else:
        print("Odd Division")


# In[3]:


import math

# Input two numbers (x and y) from the user
x = float(input("Enter the value of x: "))
y = float(input("Enter the value of y: "))

# Multiply x and y, and put the result in variable z
z = x * y

# Check if z is greater than 50
if z > 50:
    # Take the square-root of z and put it in a variable called Zsq
    Zsq = math.sqrt(z)
    
    # Check if Zsq is an even number
    if Zsq % 2 == 0:
        print("Even Square Root")
    else:
        print("Odd Square Root")
        
# if z is less than 50
else:
    # Divide z by 2 and put the result in variable Zdiv
    Zdiv = z / 2
    
    # Check if Zdiv is an even number
    if Zdiv % 2 == 0:
        print("Even Division")
    else:
        print("Odd Division")


# In[ ]:




